import { defineConfig } from 'astro/config';

export default defineConfig({
    // あなたのURL: https://<ユーザー名>.github.io/<リポジトリ名>/
    site: 'https://amufaamo.github.io',
    base: '/astro', // リポジトリ名を / で始めて書く
});